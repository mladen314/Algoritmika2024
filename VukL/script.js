function convertToMillimeters(centimeters) {
    return centimeters * 10;  
  }
  
  let centimeters = 5;  
  let millimeters = convertToMillimeters(centimeters);
  
  console.log(`${centimeters} centimeters is equal to ${millimeters} millimeters.`);

  